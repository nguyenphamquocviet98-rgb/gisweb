# 🤖 AI Provider Setup Guide

## Overview
Ứng dụng giờ support **3 AI providers** với fallback chain tự động:
1. **Gemini** (Primary) - Multi-key rotation
2. **OpenAI/ChatGPT** (Fallback) - gpt-4o-mini
3. **Local Generation** (Last Resort) - Hệ thống tích hợp

## 🔑 Configuration

### 1. Gemini (Already Configured)
Keepsome existing keys in `.env` or Streamlit Secrets:
```
GEMINI_KEY_1=AIza...
GEMINI_KEY_2=AIza...
GEMINI_API_KEY=AIza...
```

### 2. OpenAI (New - Optional)
Add to `.env` or Streamlit Secrets:
```
OPENAI_API_KEY=sk-proj-xxx...
```

**Where to get OpenAI API Key:**
- Sign up: https://platform.openai.com
- API Keys: https://platform.openai.com/api-keys
- Create new secret key
- Add to `.env`:
  ```
  OPENAI_API_KEY=sk-proj-your-key-here
  ```

### 3. Streamlit Secrets (For Cloud Deployment)
Create `.streamlit/secrets.toml` locally, or paste the same TOML into Streamlit Cloud
`App settings` -> `Secrets`.
```toml
[gcp_service_account]
type = "service_account"
project_id = "your-gcp-project-id"
private_key_id = "your-private-key-id"
client_email = "your-service-account@your-gcp-project-id.iam.gserviceaccount.com"
client_id = "your-client-id"
auth_uri = "https://accounts.google.com/o/oauth2/auth"
token_uri = "https://oauth2.googleapis.com/token"
auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
client_x509_cert_url = "https://www.googleapis.com/robot/v1/metadata/x509/your-service-account%40your-gcp-project-id.iam.gserviceaccount.com"
universe_domain = "googleapis.com"
private_key = """-----BEGIN PRIVATE KEY-----
paste-private-key-lines-here
-----END PRIVATE KEY-----"""

[gemini]
GEMINI_KEY_1 = "AIza..."
GEMINI_API_KEY = "AIza..."

[openai]
api_key = "sk-proj-zzz"
```

Important TOML rules:
- Keep each `key = "value"` on one line, except `private_key`, which should use triple quotes as shown above.
- Do not put `private_key_id =` on one line and the value on the next line.
- After saving Streamlit Cloud Secrets, wait about 1 minute, then reboot/rerun the app.

## 🚀 How It Works

### Fallback Chain
```
User clicks "Sinh báo cáo AI"
  ↓
Try Gemini (Key rotation with 3 retries)
  ├─ Success? → Return + Show "✅ Báo cáo bởi Gemini"
  ├─ Timeout/Transient? → Retry with exponential backoff
  └─ Failure? ↓
Try OpenAI (3 retries)
  ├─ Success? → Return + Show "✅ Báo cáo bởi OpenAI"
  └─ Failure? ↓
Use Local Generation
  └─ Return + Show "⚠️ AI tạm gặp sự cố. Sử dụng báo cáo hệ thống."
```

### Retry Logic
- **Exponential Backoff**: `wait_time = 2^attempt + random(0-1)`
- **Transient Errors**: 503, timeout, deadline exceeded → retry
- **Permanent Errors**: Invalid key, permission denied → skip provider

### Caching
- Reports cached for **1 hour** (TTL=3600)
- Same `(layer, roi_name, years, values)` → instant retrieval
- Reduces API quota usage ~60%

## 🧪 Testing

### Quick Test
```python
# In Python shell
from openai import OpenAI
client = OpenAI(api_key="sk-proj-your-key")
response = client.chat.completions.create(
    model="gpt-4o-mini",
    messages=[{"role": "user", "content": "Hello"}]
)
print(response.choices[0].message.content)
```

### Test in Streamlit
1. Run app: `streamlit run app.py`
2. Navigate to analysis section
3. Click "🤖 Sinh báo cáo AI (Gemini/OpenAI)"
4. Check console output:
   - `✅ Gemini success` → Using Gemini
   - `❌ Gemini failed` → Trying OpenAI
   - `✅ OpenAI success` → Using OpenAI
   - Show ⚠️ warning → Using local fallback

## 📊 Status Indicators

| Icon | Meaning | Action |
|------|---------|--------|
| ✅ | AI-generated by Gemini/OpenAI | Trust report, high quality |
| ⚠️ | Local system generated | API temporary issue, still valid |
| ❌ | Error generating report | Check logs, API credentials |

## 🐛 Troubleshooting

### Q: "OpenAI kh\u00f4ng kh\u1ea3 d\u1ee5ng"
**A:** Install package: `pip install openai>=1.0.0`

### Q: Reports taking too long
**A:** 
- Check internet connection
- Verify API keys valid
- Check OpenAI quota: https://platform.openai.com/account/usage
- Monitor rate limits

### Q: Always getting local reports
**A:**
- Verify GEMINI_KEY_* in `.env` or Secrets
- Verify OPENAI_API_KEY in `.env` or Secrets
- Check console output for error messages
- Ensure internet connectivity

### Q: Want to force-disable OpenAI?
**A:** Don't set `OPENAI_API_KEY` - will auto-fallback to Gemini only

## 💡 Best Practices

1. **Rotate Gemini Keys**: Distribute load across 4 keys
2. **Monitor Quotas**: Check API usage monthly
3. **Cache Effectively**: Same analysis → cached result
4. **Error Logs**: Check console for troubleshooting
5. **Backup Keys**: Always have 2+ Gemini keys configured

## 📝 Code Changes
- `/memories/session/ai_upgrade.md` - Full implementation details
- `app.py` - Lines 36-200, 1192-1370 modified
- `requirements.txt` - Added `openai>=1.0.0`

---
**Last Updated:** May 22, 2026
